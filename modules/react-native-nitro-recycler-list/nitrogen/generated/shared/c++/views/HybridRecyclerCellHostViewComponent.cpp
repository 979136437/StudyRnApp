///
/// HybridRecyclerCellHostViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridRecyclerCellHostViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::recyclerlist::views {

  extern const char HybridRecyclerCellHostViewComponentName[] = "RecyclerCellHostView";

  HybridRecyclerCellHostViewProps::HybridRecyclerCellHostViewProps(const react::PropsParserContext& context,
                                                                   const HybridRecyclerCellHostViewProps& sourceProps,
                                                                   const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    listId([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("listId", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.listId;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.listId);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerCellHostView.listId: ") + exc.what());
      }
    }()),
    slotId([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("slotId", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.slotId;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.slotId);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerCellHostView.slotId: ") + exc.what());
      }
    }()),
    itemKey([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("itemKey", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.itemKey;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.itemKey);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerCellHostView.itemKey: ") + exc.what());
      }
    }()),
    itemType([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("itemType", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.itemType;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.itemType);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerCellHostView.itemType: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridRecyclerCellHostViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridRecyclerCellHostViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerCellHostView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridRecyclerCellHostViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("listId"): return true;
      case hashString("slotId"): return true;
      case hashString("itemKey"): return true;
      case hashString("itemType"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridRecyclerCellHostViewComponentDescriptor::HybridRecyclerCellHostViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser()) {}

  std::shared_ptr<const react::Props> HybridRecyclerCellHostViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                                const std::shared_ptr<const react::Props>& props,
                                                                                                react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridRecyclerCellHostViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridRecyclerCellHostViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridRecyclerCellHostViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridRecyclerCellHostViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridRecyclerCellHostViewProps>& props = std::const_pointer_cast<HybridRecyclerCellHostViewProps>(constProps);
    HybridRecyclerCellHostViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::recyclerlist::views
