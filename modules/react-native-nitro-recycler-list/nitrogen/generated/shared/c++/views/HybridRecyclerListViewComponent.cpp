///
/// HybridRecyclerListViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridRecyclerListViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::recyclerlist::views {

  extern const char HybridRecyclerListViewComponentName[] = "RecyclerListView";

  HybridRecyclerListViewProps::HybridRecyclerListViewProps(const react::PropsParserContext& context,
                                                           const HybridRecyclerListViewProps& sourceProps,
                                                           const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    listId([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("listId", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.listId;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.listId);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.listId: ") + exc.what());
      }
    }()),
    descriptors([&]() -> CachedProp<std::vector<ItemDescriptor>> {
      try {
        const react::RawValue* rawValue = rawProps.at("descriptors", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.descriptors;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::vector<ItemDescriptor>>::fromRawValue(*runtime, value, sourceProps.descriptors);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.descriptors: ") + exc.what());
      }
    }()),
    layout([&]() -> CachedProp<RecyclerLayout> {
      try {
        const react::RawValue* rawValue = rawProps.at("layout", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.layout;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<RecyclerLayout>::fromRawValue(*runtime, value, sourceProps.layout);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.layout: ") + exc.what());
      }
    }()),
    horizontal([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("horizontal", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.horizontal;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.horizontal);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.horizontal: ") + exc.what());
      }
    }()),
    numColumns([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("numColumns", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.numColumns;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.numColumns);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.numColumns: ") + exc.what());
      }
    }()),
    overscan([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("overscan", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.overscan;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.overscan);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.overscan: ") + exc.what());
      }
    }()),
    refreshing([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("refreshing", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.refreshing;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.refreshing);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.refreshing: ") + exc.what());
      }
    }()),
    refreshEnabled([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("refreshEnabled", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.refreshEnabled;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.refreshEnabled);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.refreshEnabled: ") + exc.what());
      }
    }()),
    refreshThreshold([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("refreshThreshold", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.refreshThreshold;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.refreshThreshold);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.refreshThreshold: ") + exc.what());
      }
    }()),
    endReachedThreshold([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("endReachedThreshold", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.endReachedThreshold;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.endReachedThreshold);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.endReachedThreshold: ") + exc.what());
      }
    }()),
    endReachedEnabled([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("endReachedEnabled", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.endReachedEnabled;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.endReachedEnabled);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.endReachedEnabled: ") + exc.what());
      }
    }()),
    onSlotsChanged([&]() -> CachedProp<std::function<void(const std::vector<SlotBinding>& /* bindings */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onSlotsChanged", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onSlotsChanged;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void(const std::vector<SlotBinding>& /* bindings */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onSlotsChanged);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.onSlotsChanged: ") + exc.what());
      }
    }()),
    onRefreshRequested([&]() -> CachedProp<std::function<void()>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onRefreshRequested", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onRefreshRequested;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void()>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onRefreshRequested);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.onRefreshRequested: ") + exc.what());
      }
    }()),
    onRefreshPhaseChanged([&]() -> CachedProp<std::function<void(NativeRefreshPhase /* phase */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onRefreshPhaseChanged", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onRefreshPhaseChanged;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void(NativeRefreshPhase /* phase */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onRefreshPhaseChanged);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.onRefreshPhaseChanged: ") + exc.what());
      }
    }()),
    onEndReached([&]() -> CachedProp<std::function<void()>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onEndReached", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onEndReached;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void()>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onEndReached);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.onEndReached: ") + exc.what());
      }
    }()),
    onVisibleRangeChanged([&]() -> CachedProp<std::function<void(const VisibleRange& /* range */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onVisibleRangeChanged", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onVisibleRangeChanged;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void(const VisibleRange& /* range */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onVisibleRangeChanged);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.onVisibleRangeChanged: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridRecyclerListViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridRecyclerListViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RecyclerListView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridRecyclerListViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("listId"): return true;
      case hashString("descriptors"): return true;
      case hashString("layout"): return true;
      case hashString("horizontal"): return true;
      case hashString("numColumns"): return true;
      case hashString("overscan"): return true;
      case hashString("refreshing"): return true;
      case hashString("refreshEnabled"): return true;
      case hashString("refreshThreshold"): return true;
      case hashString("endReachedThreshold"): return true;
      case hashString("endReachedEnabled"): return true;
      case hashString("onSlotsChanged"): return true;
      case hashString("onRefreshRequested"): return true;
      case hashString("onRefreshPhaseChanged"): return true;
      case hashString("onEndReached"): return true;
      case hashString("onVisibleRangeChanged"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridRecyclerListViewComponentDescriptor::HybridRecyclerListViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser()) {}

  std::shared_ptr<const react::Props> HybridRecyclerListViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                            const std::shared_ptr<const react::Props>& props,
                                                                                            react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridRecyclerListViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridRecyclerListViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridRecyclerListViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridRecyclerListViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridRecyclerListViewProps>& props = std::const_pointer_cast<HybridRecyclerListViewProps>(constProps);
    HybridRecyclerListViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::recyclerlist::views
