///
/// HybridRecyclerListViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridRecyclerListViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridRecyclerListViewSpecSwift.hpp"
#import "NitroRecyclerList-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::recyclerlist;
using namespace margelo::nitro::recyclerlist::views;

/**
 * Represents the React Native View holder for the Nitro "RecyclerListView" HybridView.
 */
@interface HybridRecyclerListViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridRecyclerListViewComponent {
  std::shared_ptr<HybridRecyclerListViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridRecyclerListViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridRecyclerListViewComponentDescriptor>();
}

- (void)mountChildComponentView:(UIView<RCTComponentViewProtocol> *)childComponentView index:(NSInteger)index {
  [super mountChildComponentView:childComponentView index:index];
  if ([childComponentView isKindOfClass:[RCTViewComponentView class]]) {
    UIView* contentView = ((RCTViewComponentView*)childComponentView).contentView;
    SEL selector = NSSelectorFromString(@"nitroRecyclerComponentDidMount");
    if ([contentView respondsToSelector:selector]) {
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Warc-performSelector-leaks"
      [contentView performSelector:selector];
#pragma clang diagnostic pop
    }
  }
}

- (void)unmountChildComponentView:(UIView<RCTComponentViewProtocol> *)childComponentView index:(NSInteger)index {
  [childComponentView removeFromSuperview];
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridRecyclerListViewSpec> hybridView = NitroRecyclerList::NitroRecyclerListAutolinking::createRecyclerListView();
    _hybridView = std::dynamic_pointer_cast<HybridRecyclerListViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroRecyclerList::HybridRecyclerListViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridRecyclerListViewProps const>(props);
  auto& newViewProps = const_cast<HybridRecyclerListViewProps&>(newViewPropsConst);
  NitroRecyclerList::HybridRecyclerListViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // listId: string
  if (newViewProps.listId.isDirty) {
    swiftPart.setListId(newViewProps.listId.value);
    newViewProps.listId.isDirty = false;
  }
  // descriptors: array
  if (newViewProps.descriptors.isDirty) {
    swiftPart.setDescriptors(newViewProps.descriptors.value);
    newViewProps.descriptors.isDirty = false;
  }
  // layout: enum
  if (newViewProps.layout.isDirty) {
    swiftPart.setLayout(static_cast<int>(newViewProps.layout.value));
    newViewProps.layout.isDirty = false;
  }
  // horizontal: boolean
  if (newViewProps.horizontal.isDirty) {
    swiftPart.setHorizontal(newViewProps.horizontal.value);
    newViewProps.horizontal.isDirty = false;
  }
  // numColumns: number
  if (newViewProps.numColumns.isDirty) {
    swiftPart.setNumColumns(newViewProps.numColumns.value);
    newViewProps.numColumns.isDirty = false;
  }
  // overscan: number
  if (newViewProps.overscan.isDirty) {
    swiftPart.setOverscan(newViewProps.overscan.value);
    newViewProps.overscan.isDirty = false;
  }
  // refreshing: boolean
  if (newViewProps.refreshing.isDirty) {
    swiftPart.setRefreshing(newViewProps.refreshing.value);
    newViewProps.refreshing.isDirty = false;
  }
  // refreshEnabled: boolean
  if (newViewProps.refreshEnabled.isDirty) {
    swiftPart.setRefreshEnabled(newViewProps.refreshEnabled.value);
    newViewProps.refreshEnabled.isDirty = false;
  }
  // refreshThreshold: number
  if (newViewProps.refreshThreshold.isDirty) {
    swiftPart.setRefreshThreshold(newViewProps.refreshThreshold.value);
    newViewProps.refreshThreshold.isDirty = false;
  }
  // secondLevelEnabled: boolean
  if (newViewProps.secondLevelEnabled.isDirty) {
    swiftPart.setSecondLevelEnabled(newViewProps.secondLevelEnabled.value);
    newViewProps.secondLevelEnabled.isDirty = false;
  }
  // secondLevelOpen: boolean
  if (newViewProps.secondLevelOpen.isDirty) {
    swiftPart.setSecondLevelOpen(newViewProps.secondLevelOpen.value);
    newViewProps.secondLevelOpen.isDirty = false;
  }
  // secondLevelThreshold: number
  if (newViewProps.secondLevelThreshold.isDirty) {
    swiftPart.setSecondLevelThreshold(newViewProps.secondLevelThreshold.value);
    newViewProps.secondLevelThreshold.isDirty = false;
  }
  // tabCoordinatorId: string
  if (newViewProps.tabCoordinatorId.isDirty) {
    swiftPart.setTabCoordinatorId(newViewProps.tabCoordinatorId.value);
    newViewProps.tabCoordinatorId.isDirty = false;
  }
  // tabKey: string
  if (newViewProps.tabKey.isDirty) {
    swiftPart.setTabKey(newViewProps.tabKey.value);
    newViewProps.tabKey.isDirty = false;
  }
  // tabActive: boolean
  if (newViewProps.tabActive.isDirty) {
    swiftPart.setTabActive(newViewProps.tabActive.value);
    newViewProps.tabActive.isDirty = false;
  }
  // tabCollapseRange: number
  if (newViewProps.tabCollapseRange.isDirty) {
    swiftPart.setTabCollapseRange(newViewProps.tabCollapseRange.value);
    newViewProps.tabCollapseRange.isDirty = false;
  }
  // endReachedThreshold: number
  if (newViewProps.endReachedThreshold.isDirty) {
    swiftPart.setEndReachedThreshold(newViewProps.endReachedThreshold.value);
    newViewProps.endReachedThreshold.isDirty = false;
  }
  // endReachedEnabled: boolean
  if (newViewProps.endReachedEnabled.isDirty) {
    swiftPart.setEndReachedEnabled(newViewProps.endReachedEnabled.value);
    newViewProps.endReachedEnabled.isDirty = false;
  }
  // onSlotsChanged: function
  if (newViewProps.onSlotsChanged.isDirty) {
    swiftPart.setOnSlotsChanged(newViewProps.onSlotsChanged.value);
    newViewProps.onSlotsChanged.isDirty = false;
  }
  // onRefreshRequested: function
  if (newViewProps.onRefreshRequested.isDirty) {
    swiftPart.setOnRefreshRequested(newViewProps.onRefreshRequested.value);
    newViewProps.onRefreshRequested.isDirty = false;
  }
  // onRefreshPhaseChanged: function
  if (newViewProps.onRefreshPhaseChanged.isDirty) {
    swiftPart.setOnRefreshPhaseChanged(newViewProps.onRefreshPhaseChanged.value);
    newViewProps.onRefreshPhaseChanged.isDirty = false;
  }
  // onSecondLevelRequested: function
  if (newViewProps.onSecondLevelRequested.isDirty) {
    swiftPart.setOnSecondLevelRequested(newViewProps.onSecondLevelRequested.value);
    newViewProps.onSecondLevelRequested.isDirty = false;
  }
  // onSecondLevelPhaseChanged: function
  if (newViewProps.onSecondLevelPhaseChanged.isDirty) {
    swiftPart.setOnSecondLevelPhaseChanged(newViewProps.onSecondLevelPhaseChanged.value);
    newViewProps.onSecondLevelPhaseChanged.isDirty = false;
  }
  // onEndReached: function
  if (newViewProps.onEndReached.isDirty) {
    swiftPart.setOnEndReached(newViewProps.onEndReached.value);
    newViewProps.onEndReached.isDirty = false;
  }
  // onVisibleRangeChanged: function
  if (newViewProps.onVisibleRangeChanged.isDirty) {
    swiftPart.setOnVisibleRangeChanged(newViewProps.onVisibleRangeChanged.value);
    newViewProps.onVisibleRangeChanged.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroRecyclerList::NitroRecyclerListAutolinking::isRecyclerListViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  NitroRecyclerList::HybridRecyclerListViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  NitroRecyclerList::HybridRecyclerListViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
