///
/// HybridRecyclerCellHostViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridRecyclerCellHostViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridRecyclerCellHostViewSpecSwift.hpp"
#import "NitroRecyclerList-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::recyclerlist;
using namespace margelo::nitro::recyclerlist::views;

/**
 * Represents the React Native View holder for the Nitro "RecyclerCellHostView" HybridView.
 */
@interface HybridRecyclerCellHostViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridRecyclerCellHostViewComponent {
  std::shared_ptr<HybridRecyclerCellHostViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridRecyclerCellHostViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridRecyclerCellHostViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridRecyclerCellHostViewSpec> hybridView = NitroRecyclerList::NitroRecyclerListAutolinking::createRecyclerCellHostView();
    _hybridView = std::dynamic_pointer_cast<HybridRecyclerCellHostViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroRecyclerList::HybridRecyclerCellHostViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridRecyclerCellHostViewProps const>(props);
  auto& newViewProps = const_cast<HybridRecyclerCellHostViewProps&>(newViewPropsConst);
  NitroRecyclerList::HybridRecyclerCellHostViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // listId: string
  if (newViewProps.listId.isDirty) {
    swiftPart.setListId(newViewProps.listId.value);
    newViewProps.listId.isDirty = false;
  }
  // slotId: number
  if (newViewProps.slotId.isDirty) {
    swiftPart.setSlotId(newViewProps.slotId.value);
    newViewProps.slotId.isDirty = false;
  }
  // itemKey: string
  if (newViewProps.itemKey.isDirty) {
    swiftPart.setItemKey(newViewProps.itemKey.value);
    newViewProps.itemKey.isDirty = false;
  }
  // itemType: string
  if (newViewProps.itemType.isDirty) {
    swiftPart.setItemType(newViewProps.itemType.value);
    newViewProps.itemType.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroRecyclerList::NitroRecyclerListAutolinking::isRecyclerCellHostViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  NitroRecyclerList::HybridRecyclerCellHostViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  NitroRecyclerList::HybridRecyclerCellHostViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
