///
/// JHybridRecyclerListViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridRecyclerListViewStateUpdater.hpp"
#include "views/HybridRecyclerListViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::recyclerlist::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridRecyclerListViewState>;

void JHybridRecyclerListViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridRecyclerListViewSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  std::shared_ptr<JHybridRecyclerListViewSpec> hybridView = javaView->getJHybridRecyclerListViewSpec();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};
  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridRecyclerListViewState& data = concreteState->getData();
  const std::shared_ptr<HybridRecyclerListViewProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    // Props aren't set yet!
    throw std::runtime_error("HybridRecyclerListViewState's data doesn't contain any props!");
  }

  // Update all props if they are dirty
  if (props->listId.isDirty) {
    hybridView->setListId(props->listId.value);
    props->listId.isDirty = false;
  }
  if (props->descriptors.isDirty) {
    hybridView->setDescriptors(props->descriptors.value);
    props->descriptors.isDirty = false;
  }
  if (props->layout.isDirty) {
    hybridView->setLayout(props->layout.value);
    props->layout.isDirty = false;
  }
  if (props->horizontal.isDirty) {
    hybridView->setHorizontal(props->horizontal.value);
    props->horizontal.isDirty = false;
  }
  if (props->numColumns.isDirty) {
    hybridView->setNumColumns(props->numColumns.value);
    props->numColumns.isDirty = false;
  }
  if (props->overscan.isDirty) {
    hybridView->setOverscan(props->overscan.value);
    props->overscan.isDirty = false;
  }
  if (props->refreshing.isDirty) {
    hybridView->setRefreshing(props->refreshing.value);
    props->refreshing.isDirty = false;
  }
  if (props->refreshEnabled.isDirty) {
    hybridView->setRefreshEnabled(props->refreshEnabled.value);
    props->refreshEnabled.isDirty = false;
  }
  if (props->refreshThreshold.isDirty) {
    hybridView->setRefreshThreshold(props->refreshThreshold.value);
    props->refreshThreshold.isDirty = false;
  }
  if (props->secondLevelEnabled.isDirty) {
    hybridView->setSecondLevelEnabled(props->secondLevelEnabled.value);
    props->secondLevelEnabled.isDirty = false;
  }
  if (props->secondLevelOpen.isDirty) {
    hybridView->setSecondLevelOpen(props->secondLevelOpen.value);
    props->secondLevelOpen.isDirty = false;
  }
  if (props->secondLevelThreshold.isDirty) {
    hybridView->setSecondLevelThreshold(props->secondLevelThreshold.value);
    props->secondLevelThreshold.isDirty = false;
  }
  if (props->tabCoordinatorId.isDirty) {
    hybridView->setTabCoordinatorId(props->tabCoordinatorId.value);
    props->tabCoordinatorId.isDirty = false;
  }
  if (props->tabKey.isDirty) {
    hybridView->setTabKey(props->tabKey.value);
    props->tabKey.isDirty = false;
  }
  if (props->tabActive.isDirty) {
    hybridView->setTabActive(props->tabActive.value);
    props->tabActive.isDirty = false;
  }
  if (props->tabCollapseRange.isDirty) {
    hybridView->setTabCollapseRange(props->tabCollapseRange.value);
    props->tabCollapseRange.isDirty = false;
  }
  if (props->endReachedThreshold.isDirty) {
    hybridView->setEndReachedThreshold(props->endReachedThreshold.value);
    props->endReachedThreshold.isDirty = false;
  }
  if (props->endReachedEnabled.isDirty) {
    hybridView->setEndReachedEnabled(props->endReachedEnabled.value);
    props->endReachedEnabled.isDirty = false;
  }
  if (props->onSlotsChanged.isDirty) {
    hybridView->setOnSlotsChanged(props->onSlotsChanged.value);
    props->onSlotsChanged.isDirty = false;
  }
  if (props->onRefreshRequested.isDirty) {
    hybridView->setOnRefreshRequested(props->onRefreshRequested.value);
    props->onRefreshRequested.isDirty = false;
  }
  if (props->onRefreshPhaseChanged.isDirty) {
    hybridView->setOnRefreshPhaseChanged(props->onRefreshPhaseChanged.value);
    props->onRefreshPhaseChanged.isDirty = false;
  }
  if (props->onSecondLevelRequested.isDirty) {
    hybridView->setOnSecondLevelRequested(props->onSecondLevelRequested.value);
    props->onSecondLevelRequested.isDirty = false;
  }
  if (props->onSecondLevelPhaseChanged.isDirty) {
    hybridView->setOnSecondLevelPhaseChanged(props->onSecondLevelPhaseChanged.value);
    props->onSecondLevelPhaseChanged.isDirty = false;
  }
  if (props->onEndReached.isDirty) {
    hybridView->setOnEndReached(props->onEndReached.value);
    props->onEndReached.isDirty = false;
  }
  if (props->onVisibleRangeChanged.isDirty) {
    hybridView->setOnVisibleRangeChanged(props->onVisibleRangeChanged.value);
    props->onVisibleRangeChanged.isDirty = false;
  }

  // Update hybridRef if it changed
  if (props->hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props->hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
    props->hybridRef.isDirty = false;
  }
}

} // namespace margelo::nitro::recyclerlist::views
