///
/// JHybridRecyclerCellHostViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridRecyclerCellHostViewStateUpdater.hpp"
#include "views/HybridRecyclerCellHostViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::recyclerlist::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridRecyclerCellHostViewState>;

void JHybridRecyclerCellHostViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridRecyclerCellHostViewSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  std::shared_ptr<JHybridRecyclerCellHostViewSpec> hybridView = javaView->getJHybridRecyclerCellHostViewSpec();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};
  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridRecyclerCellHostViewState& data = concreteState->getData();
  const std::shared_ptr<HybridRecyclerCellHostViewProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    // Props aren't set yet!
    throw std::runtime_error("HybridRecyclerCellHostViewState's data doesn't contain any props!");
  }

  // Update all props if they are dirty
  if (props->listId.isDirty) {
    hybridView->setListId(props->listId.value);
    props->listId.isDirty = false;
  }
  if (props->slotId.isDirty) {
    hybridView->setSlotId(props->slotId.value);
    props->slotId.isDirty = false;
  }
  if (props->itemKey.isDirty) {
    hybridView->setItemKey(props->itemKey.value);
    props->itemKey.isDirty = false;
  }
  if (props->itemType.isDirty) {
    hybridView->setItemType(props->itemType.value);
    props->itemType.isDirty = false;
  }

  // Update hybridRef if it changed
  if (props->hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props->hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
    props->hybridRef.isDirty = false;
  }
}

} // namespace margelo::nitro::recyclerlist::views
