///
/// HybridVisibilityObserverViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridVisibilityObserverViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::visibilityobserver::views {

  extern const char HybridVisibilityObserverViewComponentName[] = "VisibilityObserverView";

  HybridVisibilityObserverViewProps::HybridVisibilityObserverViewProps(const react::PropsParserContext& context,
                                                                       const HybridVisibilityObserverViewProps& sourceProps,
                                                                       const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    enabled([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("enabled", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.enabled;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.enabled);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VisibilityObserverView.enabled: ") + exc.what());
      }
    }()),
    threshold([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("threshold", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.threshold;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.threshold);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VisibilityObserverView.threshold: ") + exc.what());
      }
    }()),
    minimumVisibleDurationMs([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("minimumVisibleDurationMs", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.minimumVisibleDurationMs;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.minimumVisibleDurationMs);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VisibilityObserverView.minimumVisibleDurationMs: ") + exc.what());
      }
    }()),
    measurementIntervalMs([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("measurementIntervalMs", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.measurementIntervalMs;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.measurementIntervalMs);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VisibilityObserverView.measurementIntervalMs: ") + exc.what());
      }
    }()),
    onVisibilityChange([&]() -> CachedProp<std::function<void(const NativeVisibilityChangeEvent& /* event */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onVisibilityChange", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onVisibilityChange;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void(const NativeVisibilityChangeEvent& /* event */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onVisibilityChange);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VisibilityObserverView.onVisibilityChange: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridVisibilityObserverViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridVisibilityObserverViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VisibilityObserverView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridVisibilityObserverViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("enabled"): return true;
      case hashString("threshold"): return true;
      case hashString("minimumVisibleDurationMs"): return true;
      case hashString("measurementIntervalMs"): return true;
      case hashString("onVisibilityChange"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridVisibilityObserverViewComponentDescriptor::HybridVisibilityObserverViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser()) {}

  std::shared_ptr<const react::Props> HybridVisibilityObserverViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                                  const std::shared_ptr<const react::Props>& props,
                                                                                                  react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridVisibilityObserverViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridVisibilityObserverViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridVisibilityObserverViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridVisibilityObserverViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridVisibilityObserverViewProps>& props = std::const_pointer_cast<HybridVisibilityObserverViewProps>(constProps);
    HybridVisibilityObserverViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::visibilityobserver::views
