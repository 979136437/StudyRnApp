///
/// HybridVisibilityObserverViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridVisibilityObserverViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridVisibilityObserverViewSpecSwift.hpp"
#import "NitroVisibilityObserver-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::visibilityobserver;
using namespace margelo::nitro::visibilityobserver::views;

/**
 * Represents the React Native View holder for the Nitro "VisibilityObserverView" HybridView.
 */
@interface HybridVisibilityObserverViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridVisibilityObserverViewComponent {
  std::shared_ptr<HybridVisibilityObserverViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridVisibilityObserverViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridVisibilityObserverViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridVisibilityObserverViewSpec> hybridView = NitroVisibilityObserver::NitroVisibilityObserverAutolinking::createVisibilityObserverView();
    _hybridView = std::dynamic_pointer_cast<HybridVisibilityObserverViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroVisibilityObserver::HybridVisibilityObserverViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridVisibilityObserverViewProps const>(props);
  auto& newViewProps = const_cast<HybridVisibilityObserverViewProps&>(newViewPropsConst);
  NitroVisibilityObserver::HybridVisibilityObserverViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // enabled: boolean
  if (newViewProps.enabled.isDirty) {
    swiftPart.setEnabled(newViewProps.enabled.value);
    newViewProps.enabled.isDirty = false;
  }
  // threshold: number
  if (newViewProps.threshold.isDirty) {
    swiftPart.setThreshold(newViewProps.threshold.value);
    newViewProps.threshold.isDirty = false;
  }
  // minimumVisibleDurationMs: number
  if (newViewProps.minimumVisibleDurationMs.isDirty) {
    swiftPart.setMinimumVisibleDurationMs(newViewProps.minimumVisibleDurationMs.value);
    newViewProps.minimumVisibleDurationMs.isDirty = false;
  }
  // measurementIntervalMs: number
  if (newViewProps.measurementIntervalMs.isDirty) {
    swiftPart.setMeasurementIntervalMs(newViewProps.measurementIntervalMs.value);
    newViewProps.measurementIntervalMs.isDirty = false;
  }
  // onVisibilityChange: function
  if (newViewProps.onVisibilityChange.isDirty) {
    swiftPart.setOnVisibilityChange(newViewProps.onVisibilityChange.value);
    newViewProps.onVisibilityChange.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroVisibilityObserver::NitroVisibilityObserverAutolinking::isVisibilityObserverViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  NitroVisibilityObserver::HybridVisibilityObserverViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  NitroVisibilityObserver::HybridVisibilityObserverViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
