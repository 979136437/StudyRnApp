///
/// HybridMediaThumbnailComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridMediaThumbnailComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::imagepicker::views {

  extern const char HybridMediaThumbnailComponentName[] = "MediaThumbnail";

  HybridMediaThumbnailProps::HybridMediaThumbnailProps(const react::PropsParserContext& context,
                                                       const HybridMediaThumbnailProps& sourceProps,
                                                       const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    assetId([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("assetId", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.assetId;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.assetId);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MediaThumbnail.assetId: ") + exc.what());
      }
    }()),
    resizeMode([&]() -> CachedProp<ThumbnailResizeMode> {
      try {
        const react::RawValue* rawValue = rawProps.at("resizeMode", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.resizeMode;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<ThumbnailResizeMode>::fromRawValue(*runtime, value, sourceProps.resizeMode);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MediaThumbnail.resizeMode: ") + exc.what());
      }
    }()),
    shouldDownloadFromNetwork([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("shouldDownloadFromNetwork", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.shouldDownloadFromNetwork;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.shouldDownloadFromNetwork);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MediaThumbnail.shouldDownloadFromNetwork: ") + exc.what());
      }
    }()),
    onLoad([&]() -> CachedProp<std::function<void(const ThumbnailLoadEvent& /* event */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onLoad", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onLoad;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void(const ThumbnailLoadEvent& /* event */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onLoad);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MediaThumbnail.onLoad: ") + exc.what());
      }
    }()),
    onError([&]() -> CachedProp<std::function<void(const ThumbnailErrorEvent& /* event */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onError", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onError;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void(const ThumbnailErrorEvent& /* event */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onError);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MediaThumbnail.onError: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridMediaThumbnailSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridMediaThumbnailSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MediaThumbnail.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridMediaThumbnailProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("assetId"): return true;
      case hashString("resizeMode"): return true;
      case hashString("shouldDownloadFromNetwork"): return true;
      case hashString("onLoad"): return true;
      case hashString("onError"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridMediaThumbnailComponentDescriptor::HybridMediaThumbnailComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser()) {}

  std::shared_ptr<const react::Props> HybridMediaThumbnailComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                          const std::shared_ptr<const react::Props>& props,
                                                                                          react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridMediaThumbnailShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridMediaThumbnailComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridMediaThumbnailShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridMediaThumbnailProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridMediaThumbnailProps>& props = std::const_pointer_cast<HybridMediaThumbnailProps>(constProps);
    HybridMediaThumbnailState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::imagepicker::views
