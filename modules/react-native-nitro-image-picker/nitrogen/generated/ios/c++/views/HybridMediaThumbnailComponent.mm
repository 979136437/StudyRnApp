///
/// HybridMediaThumbnailComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridMediaThumbnailComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridMediaThumbnailSpecSwift.hpp"
#import "NitroImagePicker-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::imagepicker;
using namespace margelo::nitro::imagepicker::views;

/**
 * Represents the React Native View holder for the Nitro "MediaThumbnail" HybridView.
 */
@interface HybridMediaThumbnailComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridMediaThumbnailComponent {
  std::shared_ptr<HybridMediaThumbnailSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridMediaThumbnailComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridMediaThumbnailComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridMediaThumbnailSpec> hybridView = NitroImagePicker::NitroImagePickerAutolinking::createMediaThumbnail();
    _hybridView = std::dynamic_pointer_cast<HybridMediaThumbnailSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroImagePicker::HybridMediaThumbnailSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridMediaThumbnailProps const>(props);
  auto& newViewProps = const_cast<HybridMediaThumbnailProps&>(newViewPropsConst);
  NitroImagePicker::HybridMediaThumbnailSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // assetId: string
  if (newViewProps.assetId.isDirty) {
    swiftPart.setAssetId(newViewProps.assetId.value);
    newViewProps.assetId.isDirty = false;
  }
  // resizeMode: enum
  if (newViewProps.resizeMode.isDirty) {
    swiftPart.setResizeMode(static_cast<int>(newViewProps.resizeMode.value));
    newViewProps.resizeMode.isDirty = false;
  }
  // shouldDownloadFromNetwork: boolean
  if (newViewProps.shouldDownloadFromNetwork.isDirty) {
    swiftPart.setShouldDownloadFromNetwork(newViewProps.shouldDownloadFromNetwork.value);
    newViewProps.shouldDownloadFromNetwork.isDirty = false;
  }
  // onLoad: function
  if (newViewProps.onLoad.isDirty) {
    swiftPart.setOnLoad(newViewProps.onLoad.value);
    newViewProps.onLoad.isDirty = false;
  }
  // onError: function
  if (newViewProps.onError.isDirty) {
    swiftPart.setOnError(newViewProps.onError.value);
    newViewProps.onError.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroImagePicker::NitroImagePickerAutolinking::isMediaThumbnailRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  NitroImagePicker::HybridMediaThumbnailSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  NitroImagePicker::HybridMediaThumbnailSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
