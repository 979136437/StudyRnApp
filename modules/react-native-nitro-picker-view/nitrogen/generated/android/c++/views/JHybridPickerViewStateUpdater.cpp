///
/// JHybridPickerViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridPickerViewStateUpdater.hpp"
#include "views/HybridPickerViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::pickerview::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridPickerViewState>;

void JHybridPickerViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridPickerViewSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  std::shared_ptr<JHybridPickerViewSpec> hybridView = javaView->getJHybridPickerViewSpec();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};
  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridPickerViewState& data = concreteState->getData();
  const std::shared_ptr<HybridPickerViewProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    // Props aren't set yet!
    throw std::runtime_error("HybridPickerViewState's data doesn't contain any props!");
  }

  // Update all props if they are dirty
  if (props->columns.isDirty) {
    hybridView->setColumns(props->columns.value);
    props->columns.isDirty = false;
  }
  if (props->value.isDirty) {
    hybridView->setValue(props->value.value);
    props->value.isDirty = false;
  }
  if (props->disabled.isDirty) {
    hybridView->setDisabled(props->disabled.value);
    props->disabled.isDirty = false;
  }
  if (props->itemHeight.isDirty) {
    hybridView->setItemHeight(props->itemHeight.value);
    props->itemHeight.isDirty = false;
  }
  if (props->magnification.isDirty) {
    hybridView->setMagnification(props->magnification.value);
    props->magnification.isDirty = false;
  }
  if (props->edgeFadeColor.isDirty) {
    hybridView->setEdgeFadeColor(props->edgeFadeColor.value);
    props->edgeFadeColor.isDirty = false;
  }
  if (props->edgeFadeSize.isDirty) {
    hybridView->setEdgeFadeSize(props->edgeFadeSize.value);
    props->edgeFadeSize.isDirty = false;
  }
  if (props->edgeFadeIntensity.isDirty) {
    hybridView->setEdgeFadeIntensity(props->edgeFadeIntensity.value);
    props->edgeFadeIntensity.isDirty = false;
  }
  if (props->onChange.isDirty) {
    hybridView->setOnChange(props->onChange.value);
    props->onChange.isDirty = false;
  }
  if (props->onPickStart.isDirty) {
    hybridView->setOnPickStart(props->onPickStart.value);
    props->onPickStart.isDirty = false;
  }
  if (props->onPickEnd.isDirty) {
    hybridView->setOnPickEnd(props->onPickEnd.value);
    props->onPickEnd.isDirty = false;
  }

  // Update hybridRef if it changed
  if (props->hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props->hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
    props->hybridRef.isDirty = false;
  }
}

} // namespace margelo::nitro::pickerview::views
