///
/// HybridPickerViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridPickerViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::pickerview::views {

  extern const char HybridPickerViewComponentName[] = "PickerView";

  HybridPickerViewProps::HybridPickerViewProps(const react::PropsParserContext& context,
                                               const HybridPickerViewProps& sourceProps,
                                               const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    columns([&]() -> CachedProp<std::vector<NativePickerColumn>> {
      try {
        const react::RawValue* rawValue = rawProps.at("columns", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.columns;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::vector<NativePickerColumn>>::fromRawValue(*runtime, value, sourceProps.columns);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.columns: ") + exc.what());
      }
    }()),
    value([&]() -> CachedProp<std::vector<double>> {
      try {
        const react::RawValue* rawValue = rawProps.at("value", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.value;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::vector<double>>::fromRawValue(*runtime, value, sourceProps.value);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.value: ") + exc.what());
      }
    }()),
    disabled([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("disabled", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.disabled;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.disabled);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.disabled: ") + exc.what());
      }
    }()),
    itemHeight([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("itemHeight", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.itemHeight;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.itemHeight);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.itemHeight: ") + exc.what());
      }
    }()),
    magnification([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("magnification", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.magnification;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.magnification);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.magnification: ") + exc.what());
      }
    }()),
    edgeFadeColor([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("edgeFadeColor", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.edgeFadeColor;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.edgeFadeColor);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.edgeFadeColor: ") + exc.what());
      }
    }()),
    edgeFadeSize([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("edgeFadeSize", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.edgeFadeSize;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.edgeFadeSize);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.edgeFadeSize: ") + exc.what());
      }
    }()),
    edgeFadeIntensity([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("edgeFadeIntensity", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.edgeFadeIntensity;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.edgeFadeIntensity);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.edgeFadeIntensity: ") + exc.what());
      }
    }()),
    onChange([&]() -> CachedProp<std::function<void(const NativePickerEvent& /* event */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onChange", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onChange;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void(const NativePickerEvent& /* event */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onChange);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.onChange: ") + exc.what());
      }
    }()),
    onPickStart([&]() -> CachedProp<std::function<void(const NativePickerEvent& /* event */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onPickStart", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onPickStart;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void(const NativePickerEvent& /* event */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onPickStart);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.onPickStart: ") + exc.what());
      }
    }()),
    onPickEnd([&]() -> CachedProp<std::function<void(const NativePickerEvent& /* event */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onPickEnd", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onPickEnd;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void(const NativePickerEvent& /* event */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onPickEnd);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.onPickEnd: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridPickerViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridPickerViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridPickerViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("columns"): return true;
      case hashString("value"): return true;
      case hashString("disabled"): return true;
      case hashString("itemHeight"): return true;
      case hashString("magnification"): return true;
      case hashString("edgeFadeColor"): return true;
      case hashString("edgeFadeSize"): return true;
      case hashString("edgeFadeIntensity"): return true;
      case hashString("onChange"): return true;
      case hashString("onPickStart"): return true;
      case hashString("onPickEnd"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridPickerViewComponentDescriptor::HybridPickerViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser()) {}

  std::shared_ptr<const react::Props> HybridPickerViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                      const std::shared_ptr<const react::Props>& props,
                                                                                      react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridPickerViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridPickerViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridPickerViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridPickerViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridPickerViewProps>& props = std::const_pointer_cast<HybridPickerViewProps>(constProps);
    HybridPickerViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::pickerview::views
