///
/// HybridCompressorSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `CompressionResult` to properly resolve imports.
namespace margelo::nitro::compressor { struct CompressionResult; }
// Forward declaration of `ImageCompressionOptions` to properly resolve imports.
namespace margelo::nitro::compressor { struct ImageCompressionOptions; }
// Forward declaration of `VideoCompressionOptions` to properly resolve imports.
namespace margelo::nitro::compressor { struct VideoCompressionOptions; }
// Forward declaration of `ImageMetadata` to properly resolve imports.
namespace margelo::nitro::compressor { struct ImageMetadata; }
// Forward declaration of `VideoMetadata` to properly resolve imports.
namespace margelo::nitro::compressor { struct VideoMetadata; }
// Forward declaration of `ThumbnailResult` to properly resolve imports.
namespace margelo::nitro::compressor { struct ThumbnailResult; }
// Forward declaration of `ThumbnailOptions` to properly resolve imports.
namespace margelo::nitro::compressor { struct ThumbnailOptions; }

#include <string>
#include "CompressionResult.hpp"
#include <NitroModules/Promise.hpp>
#include "ImageCompressionOptions.hpp"
#include "VideoCompressionOptions.hpp"
#include "ImageMetadata.hpp"
#include "VideoMetadata.hpp"
#include "ThumbnailResult.hpp"
#include "ThumbnailOptions.hpp"

namespace margelo::nitro::compressor {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Compressor`
   * Inherit this class to create instances of `HybridCompressorSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridCompressor: public HybridCompressorSpec {
   * public:
   *   HybridCompressor(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridCompressorSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridCompressorSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridCompressorSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::string createOperationId() = 0;
      virtual std::shared_ptr<Promise<CompressionResult>> compressImage(const std::string& operationId, const std::string& source, const ImageCompressionOptions& options) = 0;
      virtual std::shared_ptr<Promise<CompressionResult>> compressVideo(const std::string& operationId, const std::string& source, const VideoCompressionOptions& options) = 0;
      virtual std::shared_ptr<Promise<ImageMetadata>> getImageMetadata(const std::string& source) = 0;
      virtual std::shared_ptr<Promise<VideoMetadata>> getVideoMetadata(const std::string& source) = 0;
      virtual std::shared_ptr<Promise<ThumbnailResult>> createVideoThumbnail(const std::string& operationId, const std::string& source, const ThumbnailOptions& options) = 0;
      virtual bool cancel(const std::string& operationId) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Compressor";
  };

} // namespace margelo::nitro::compressor
